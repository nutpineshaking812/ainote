/**
 * Supported resource types in the application.
 * Synchronize this with the database enum 'resource_type'.
 */
export const RESOURCE_TYPES = {
  FORM: 'form',
  VIEW: 'view',
  DOCUMENT: 'document',
  FOLDER: 'folder',
};
